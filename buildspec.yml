version: 0.2
 
phases:
  install:
    runtime-versions:
      java: openjdk8
  
    commands:
      - apt-get install openjdk-8-jdk
      - apt-get install -y maven
      - apt-get install unzip

  build:
    commands:
      - echo Build started on `date`
      - mvn test

  post_build:
    commands:
      - echo Build completed on `date`
      - mvn package
      - zip sparkjava-hello-world-1.0.zip ./sparkjava-hello-world-1.0.war

artifacts:
  type: 
  files:
    - target/sparkjava-hello-world-1.0.war
    - /appspec.yml
